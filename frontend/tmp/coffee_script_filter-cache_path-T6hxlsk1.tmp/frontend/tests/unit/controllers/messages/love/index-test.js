import { moduleFor, test } from 'ember-qunit';
moduleFor('controller:messages/love/index', {});

test('it exists', function(assert) {
  var controller;
  controller = this.subject();
  return assert.ok(controller);
});
