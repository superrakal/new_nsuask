import { moduleFor, test } from 'ember-qunit';
moduleFor('route:messages/love/index', 'Unit | Route | messages/love/index', {});

test('it exists', function(assert) {
  var route;
  route = this.subject();
  return assert.ok(route);
});
