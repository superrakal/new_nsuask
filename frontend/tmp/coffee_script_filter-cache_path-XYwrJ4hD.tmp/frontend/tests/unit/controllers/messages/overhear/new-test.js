import { moduleFor, test } from 'ember-qunit';
moduleFor('controller:messages/overhear/new', {});

test('it exists', function(assert) {
  var controller;
  controller = this.subject();
  return assert.ok(controller);
});
