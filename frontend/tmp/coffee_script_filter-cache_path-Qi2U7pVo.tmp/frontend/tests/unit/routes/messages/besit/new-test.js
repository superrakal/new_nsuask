import { moduleFor, test } from 'ember-qunit';
moduleFor('route:messages/besit/new', 'Unit | Route | messages/besit/new', {});

test('it exists', function(assert) {
  var route;
  route = this.subject();
  return assert.ok(route);
});
